"""
Running script for DanceCat
"""
import os
from DanceCat import app, socket_io

if __name__ == '__main__':
    # KEY_FILE = os.path.join('DanceCat.key')
    # CERT_FILE = os.path.join('DanceCat.cert')

    socket_io.run(app,
                  host='0.0.0.0',
                  port=8443,
                  debug=True,
                  # For SSL
                  # keyfile=KEY_FILE,
                  # certfile=CERT_FILE
                  )
